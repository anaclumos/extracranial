---
lang: 'en'
slug: '/D7D53C'
---

In [[Graphics]], Conditional Compilation can be used for multi-platform engine

```cpp
#if (PLATFORM == SWITCH)
	return ...
#elif (PLATFORM == XBOX)
	return ...
#endif
```
